# my-erp 工作约定

## 上下文纪律
- `docs/glossary/` 是写给人看的术语解释，**默认不读取、不 grep、不用于实现**。
  仅当用户明确指定某个词条，或需要核对某术语在本项目的约定时才读单个文件。
- 领域代码是唯一权威；文档与代码冲突时以代码为准。
- 不要为了"了解项目"而通读 `docs/`；按需读单个文件的相关段落。

## 协作分工
- Maven 工程、启动类、文件规划由用户本人完成。
- 仅在被指明具体文件逻辑时介入。